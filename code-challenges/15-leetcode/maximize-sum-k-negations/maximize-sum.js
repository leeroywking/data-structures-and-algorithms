/**
 * @param {number[]} A
 * @param {number} K
 * @return {number}
 */
var largestSumAfterKNegations = function(A, K) {
    let smallestIdx
    for(let i = 0; i < A.length ;  i++){
        if(smallest === undefined){smallest = A[i]}
    }
    
};

module.exports = largestSumAfterKNegations